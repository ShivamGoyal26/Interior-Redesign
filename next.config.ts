import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["ai-room-redesign.s3.ap-south-1.amazonaws.com"], // Replace with the domain of your image source
  },
};

export default nextConfig;
